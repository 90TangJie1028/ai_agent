# 2026-06-26 · 第 1 周 D5

**项目**：P1 Model Gateway  
**周计划**：[week-01.md](../weekly/week-01.md)

## 四时段记录

| 时段 | 计划 | 实际完成 |
|:---|:---|:---|
| 学习 30m | 各模型 pricing / usage 字段 → `notes/concepts/04-费用与用量.md` | ✅ 含复盘自答三点 |
| 编码 120m | `metrics.py` CallRecord + GatewayResult；gateway 集成 + 失败路径 | ✅ |
| 测试 45m | `tests/test_metrics.py` 断言 tokens > 0 | ✅ |
| 文档 30m | `config/pricing.yaml` | ✅ |

## 5 条笔记
1. usage 三字段来自 API，cost 是客户端按 pricing 表**折算**（非 API 字段）
2. `CallRecord` 是纯转换层，**不穿透 Adapter**——只消费已完成的 `ChatResult`
3. `GatewayResult = ChatResult | None + CallRecord`；失败吞异常、靠 `result is None` 判断
4. `summary()` 是**全局**汇总，不是按 model 分桶（❌ 原理解有误，已纠正）
5. D5 记单次，D6 bench 用 `MetricsCollector` 串成报告

## 复盘错题（必看）

| # | 原误解 | 纠正 |
|:--|:------|:-----|
| 1 | CallRecord 过滤/穿透 Adapter 链 | 只在 Gateway 出口做字段拷贝 + cost 计算 |
| 2 | summary 按每个 model 汇总 | 当前仅全局 total_tokens / total_cost / P95 |
| 3 | 失败仍抛异常 | 当前返回 `result=None` + failed record |

详见 `notes/concepts/04-费用与用量.md` → 「复盘：自答三点对照」

## 今日产物（必填其一）
- [x] git commit: W1-D5 metrics CallRecord
- [x] 其他: `notes/concepts/04-费用与用量.md`

## 指标
| 项 | 值 |
|:---|:---|
| test_metrics | 6/6 绿 |
| CallRecord.tokens | > 0（mock） |
| cost_usd | 按 pricing.yaml 可算 |

## 问题与解决

初版以为 D5「就是取 key 赋值」——价值在分层与失败路径，不在语法。自答时把 CallRecord 当成 Adapter 中间层，复盘已标记 ❌。

## 明日第一件事

实现 `cli.py bench --n 20`（D6）

## 面试素材

> Adapter 产事实（ChatResult），Gateway 出口产观测（CallRecord）；cost 是客户端策略；失败路径 record 仍完整，success_rate 靠 Collector 聚合。
