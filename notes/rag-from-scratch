# RAG From Scratch 通俗笔记

> 来源：[langchain-ai/rag-from-scratch](https://github.com/langchain-ai/rag-from-scratch)  
> 配套视频：[YouTube Playlist](https://www.youtube.com/playlist?list=PLfaIDFEXuae2LXbO1_PKyVJiQ23ZztA0x)  
> 形式：5 个 Jupyter Notebook，对应 Part 1–18  
> 整理日期：2026-07-22

---

## 一句话讲清：这仓库在讲什么？

大模型（LLM）训练时看过很多公开文本，但对**你的私有资料**、**最新消息**往往一问三不知，还容易一本正经地胡说（幻觉）。

**RAG（Retrieval-Augmented Generation，检索增强生成）** 的做法很朴素：

1. 先把你的文档整理成可搜索的知识库  
2. 用户提问时，先**检索**相关段落  
3. 把检索到的内容塞进提示词，再让模型**生成**答案  

比喻：模型是「会写总结的实习生」；RAG 是「先让他去档案柜翻资料，再写报告」，而不是让他空口背。

仓库作者（LangChain 工程师）用「从零搭一遍」的方式，把基础流水线讲透，再逐步升级到查询改写、路由、高级索引、自适应检索等进阶技巧。

---

## 为什么不用微调（Fine-tuning）硬塞知识？

README 里点出两条现实：

| 手段 | 大致作用 | 局限 |
|------|----------|------|
| 微调 | 更适合改「说话风格 / 任务格式」 | 对「死记硬背事实」往往不划算；贵、慢、难更新 |
| RAG | 把事实放在外部资料里，用时再查 | 依赖检索质量；资料脏、切得烂，答案也会烂 |

结论：**要记得住、改得动的事实 → 优先 RAG**；要改语气、改输出格式 → 才更考虑微调。两者也可以组合，但本系列主线是 RAG。

---

## 核心三步（整门课的骨架）

```
文档 → [索引 Indexing] → 向量库/数据库
              ↑
用户问题 → [检索 Retrieval] → 相关片段
              ↓
        [生成 Generation] →  grounded 答案（有依据）
```

### 1. Indexing（建索引）

把原始文档变成「可搜」：

- **加载**：PDF、网页、Markdown……读进来  
- **切分（Chunking）**：切成一段段小文本（太大塞不进上下文，太碎又丢语义）  
- **嵌入（Embedding）**：把每段文字变成一串数字向量（语义相近的向量距离近）  
- **入库**：存进向量库（系列里常见 Chroma + OpenAI Embeddings 一类组合）

### 2. Retrieval（检索）

用户问题也变成向量，在库里找「最近」的若干段（k-NN / 相似度搜索）。  
`k` 取多少：太小可能漏关键信息，太大则噪音多、浪费上下文。

### 3. Generation（生成）

把「问题 + 检索到的片段」填进 Prompt，让 LLM 基于这些材料作答。  
理想状态：答案能对应到某段原文，而不是凭空编。

---

## 仓库文件怎么对应课程？

| Notebook | 覆盖 Part | 主题（人话） |
|----------|-----------|--------------|
| `rag_from_scratch_1_to_4.ipynb` | 1–4 | 总览 + 基础三步跑通 |
| `rag_from_scratch_5_to_9.ipynb` | 5–9 | 查询变换：换个说法再搜 |
| `rag_from_scratch_10_and_11.ipynb` | 10–11 | 路由 + 结构化查询 |
| `rag_from_scratch_12_to_14.ipynb` | 12–14 | 更聪明的索引方式 |
| `rag_from_scratch_15_to_18.ipynb` | 15–18 | 重排 / 纠错 / 自检 / 长上下文冲击 |

---

## Part 1–4：把基础 RAG 跑通

### Part 1 Overview
先建立心智模型：RAG = 检索 + 生成，不是「换一个更大的模型」就能替代。

### Part 2 Indexing
动手：文档 → 切块 → embedding → 写入向量库。  
关键直觉：索引质量决定后续上限；切块策略往往比「再换一个 fancy 算法」更先见效。

### Part 3 Retrieval
问题 embedding → 相似度搜索 → 取 Top-k。  
要调的主要是：相似度度量、k、过滤条件。

### Part 4 Generation
Prompt 模板把「上下文」和「问题」拼起来，用 Chain 串起检索与生成。  
到这里，你已经有一条最小可用的 RAG 流水线。

---

## Part 5–9：查询变换（Query Transformations）

很多失败不是「库里没有」，而是**问法跟文档写法对不上**。这一段专门教：先改问题，再检索。

### Part 5 Multi Query（多查询）
同一问题，让模型改写成多个不同视角的问法，分别检索，再合并结果。  
类比：一个人搜「退货政策」，另一个人搜「多久能退」「七天无理由」——覆盖面更宽。

### Part 6 RAG-Fusion
多路检索结果用 **RRF（倒数排名融合）** 之类方法合并排序，减少「某一路偶然排第一」的偏见。

### Part 7 Decomposition（问题分解）
复杂问题拆成子问题：

- **递归作答**：子问题答案串起来推最终答案  
- **分别作答**：各子问题独立检索/回答再汇总  

适合「既要 A 又要对比 B」这类复合问。

### Part 8 Step Back（后退提问）
先问一个更抽象、更上位的问题（step-back question），检索到原则/背景，再回来答具体问题。  
类比：先查「劳动法加班通则」，再答「我司周五加班是否合规」。

### Part 9 HyDE（Hypothetical Document Embeddings）
先让模型「臆造」一篇可能的答案文档，用这篇假文档去向量库搜。  
直觉：问题向量和文档向量空间有时不对齐；「假答案」更像真实文档，更容易搜中。

---

## Part 10–11：路由与查询构造

### Part 10 Logical / Semantic Routing（路由）
资料可能来自多个源（产品文档 / 代码库 / 网页……），先决定「去哪查」：

- **逻辑路由**：用结构化输出 / function calling，明确分到某一类  
- **语义路由**：用 embedding 相似度选最像的目的地  

不路由的后果：在错误库里死磕，答案再流畅也是错的。

### Part 11 Query Construction（查询构造）
自然语言 → 带**元数据过滤**的检索（或 Text-to-SQL / Cypher 一类）。  
例：「2023 年 Q4 的财务 PDF」→ 除了语义搜，还要 `year=2023 AND quarter=Q4`。  
这是「只会向量相似度」迈向「能用结构化条件」的关键一步。

---

## Part 12–14：索引进阶

前言仍强调 **Chunking**：切块是索引的地基。

### Part 12 Multi-representation Indexing（多表征索引）
常见套路：向量库里存「摘要」方便检索；原文另存，命中后再取全文。  
好处：摘要短、噪声少；需要细节时再拿完整段落。

### Part 13 RAPTOR
对文档做聚类、摘要，再递归摘要，形成**树状/层次化**索引。  
适合长文档、需要「既看局部又看全局」的问答。

### Part 14 ColBERT
更细粒度的「迟交互」检索思路：不只比整段向量，还在 token 级做匹配。  
直觉：有时关键词对得上比「整段意思差不多」更重要。

---

## Part 15–18：检索质量、自适应与长上下文

### Part 15 Re-ranking（重排序）
先粗召回一批候选，再用更强的重排模型精排。  
流水线：「广撒网 → 精挑细选」，成本和质量之间常比「一次召回 100 条全塞进 Prompt」更划算。

### Part 16 CRAG（Corrective RAG，纠错式 RAG）
对检索结果打分：相关则用；不行则改写查询、换源，甚至退回网页搜索。  
核心：检索失败时有**纠偏回路**，而不是硬生成。

### Part 17 Self-RAG
模型（或流程）自检：这段该不该检索？检索到的有没有用？答案是否有依据？  
把「检索 / 生成 / 批判」做成可循环的控制流（系列后半常配合 LangGraph 状态机思路）。

### Part 18 Long Context 的冲击
超长上下文窗口兴起后，有人喊「RAG 死了」。这一课用实验（含 needle-in-a-haystack 一类）讨论：

- 长上下文能装更多原文，但**贵、慢、仍可能找不到针**  
- RAG 仍是控制成本、缩小搜索空间、对接私有库的实用手段  
- 现实往往是：**长上下文 + RAG 组合**，而不是二选一撕逼

---

## 建议怎么学（务实路径）

1. **先只跑 1–4**：确保本地能完成「建库 → 提问 → 有依据回答」。  
2. **再挑痛点学 5–9**：搜不准就练 Multi-Query / HyDE / Decomposition。  
3. **多数据源时看 10–11**：路由和元数据过滤往往比换 embedding 模型更救命。  
4. **文档很长再看 12–14**：多表征 / RAPTOR。  
5. **要上线稳一点再看 15–17**：重排 + 打分纠错 + 自检。  
6. **最后看 18**：建立对「长上下文能不能替代 RAG」的判断力。

动手时注意：Notebook 依赖 API Key、包版本可能偏旧，跑不通时优先对齐当前 LangChain 文档，**概念比某行 API 更保值**。

---

## 术语速查（对照英文）

| 英文 | 中文直觉 |
|------|----------|
| RAG | 先查资料再生成 |
| Chunking | 切段 |
| Embedding | 把文字变成可比较的向量 |
| Vector Store | 向量数据库 |
| Top-k / k-NN | 找最像的 k 条 |
| Prompt / In-context learning | 把证据写进提示词里让模型现学现用 |
| Hallucination | 幻觉：说得像真的但没依据 |
| Re-ranking | 粗召回后再精排 |
| Grounding | 答案要能落到证据上 |

---

## 关键链接

- 仓库：https://github.com/langchain-ai/rag-from-scratch  
- 视频列表：https://www.youtube.com/playlist?list=PLfaIDFEXuae2LXbO1_PKyVJiQ23ZztA0x  
- README 观点延伸：微调更偏「形式」而非「事实」——见 Anyscale / Glean 等业界讨论（README 内链）

---

## 一句话收尾

这套材料不是又一个「调用 `RetrievalQA` 五分钟 Demo」，而是按 **索引 → 检索 → 生成** 搭骨架，再系统补上：**改问法、选库、造查询、换索引结构、重排纠错、自检，以及长上下文时代的定位**。先跑通 1–4，其余按你真实痛点选修即可。
