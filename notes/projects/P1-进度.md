# P1 Model Gateway · 进度看板

**最后更新**：2026-07-05（W2 D9）  
**目录**：`coding/projects/01-model-gateway`

## 模块状态

| 模块 | 状态 | 说明 |
|:---|:---:|:---|
| `gateway.py` | ✅ W2 D9 | + `chat_structured(mode=)`、`response_format` 透传 |
| `structured_output.py` | ✅ D9 | prompt / response_format 构建、parse |
| `adapters/deepseek.py` | ✅ | 主模型，OpenAI 兼容 |
| `adapters/openai_compat.py` | ✅ D9 | + `response_format` 透传 |
| `adapters/mock.py` | ✅ D6 | 离线 bench、故意失败注入 |
| `adapters/moonshot.py` | ⬜ W2 | 文件在，待 D12 接通 |
| `retry.py` | ✅ D3 | `RetryPolicy`、`@with_retry` |
| `ratelimit.py` | ✅ D4 | `TokenBucket`、`async acquire` |
| `metrics.py` | ✅ D5 | `CallRecord`、`MetricsCollector`、`compute_cost_usd` |
| `bench.py` | ✅ D6 | 批量调用 + JSON 报告 |
| `cli.py` | ✅ | `chat`、`providers`、`bench` |
| `config.py` + `pricing.yaml` | ✅ | 环境变量、单价表 |
| `schemas.py` | ✅ D8 | `ChatRequest`、`StructuredAnswer` |
| `tools/registry.py` | ⬜ W2 D10 | Function Calling 注册 |
| `tools/calculator.py` | ⬜ W2 D11 | 计算器 E2E |

## 测试覆盖（肉眼）

| 文件 | 测什么 |
|:---|:---|
| `test_retry.py` | 429 退避、非重试异常、timeout |
| `test_ratelimit.py` | 令牌桶 QPS、burst、Semaphore 并发 |
| `test_metrics.py` | CallRecord 字段、Collector 聚合、cost 计算 |
| `test_bench.py` | build_calls、报告结构、故意失败 |
| `test_mock_adapter.py` | 空 prompt、fail marker |
| `test_gateway_smoke.py` | provider 列表、mock chat、integration 可选 |
| `test_schemas.py` | JSON Schema 快照、三种 mode、response_format 透传 |

## W1 → W2 衔接

```text
已完成：调用 → 重试/限流 → 单次观测 → 批量 bench
待做：  ToolRegistry → FC 闭环 → 多模型路由
```

## 已知坑

- DeepSeek 空 prompt 不一定失败；bench 真实 API 用无效 model 注入失败
- 默认 bench provider 是 `mock`，CI/离线友好
- `GatewayResult` 与裸 `str` 不兼容——旧代码需改 `.result.content`
